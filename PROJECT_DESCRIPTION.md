# Полное описание проекта UB (WB Analytics Platform)

## Цель проекта

**UB (WB Analytics Platform)** — это полнофункциональная платформа для анализа и мониторинга товаров маркетплейса Wildberries, предназначенная для продавцов и маркетологов. Система позволяет отслеживать динамику цен конкурентов, анализировать отзывы покупателей с помощью искусственного интеллекта для выявления проблемных зон товаров и получения стратегических рекомендаций по улучшению продукта, а также вести историю изменений цен с возможностью экспорта отчетов в PDF формате. Платформа работает как Telegram Web App, обеспечивая удобный доступ с мобильных устройств и интеграцию с экосистемой Telegram.

---

## Стек технологий

### Языки программирования:
- **Python 3.9+** — основной язык backend
- **JavaScript (ES6+)** — язык frontend
- **SQL** — запросы к базе данных

### Backend фреймворки и библиотеки:
- **FastAPI** — современный асинхронный веб-фреймворк для создания REST API
- **Uvicorn** — ASGI сервер для запуска FastAPI приложения
- **SQLAlchemy** — ORM для работы с базой данных (async и sync версии)
- **Celery** — распределенная очередь задач для фоновой обработки
- **Selenium** — автоматизация браузера для парсинга динамического контента
- **Pydantic** — валидация данных и схемы (используется через FastAPI)
- **FPDF2** — генерация PDF отчетов
- **python-dotenv** — управление переменными окружения
- **aiohttp** — асинхронный HTTP клиент для внутренних API запросов
- **requests** — синхронный HTTP клиент (для AI API и WB API)
- **asyncpg** — асинхронный драйвер PostgreSQL
- **psycopg2-binary** — синхронный драйвер PostgreSQL (для Celery)

### Frontend фреймворки и библиотеки:
- **React 18** — библиотека для создания пользовательского интерфейса
- **Vite** — сборщик и dev-сервер (альтернатива Create React App)
- **Tailwind CSS** — utility-first CSS фреймворк для стилизации
- **PostCSS** — пост-процессор CSS с поддержкой Autoprefixer
- **Lucide React** — библиотека иконок
- **Recharts** — библиотека для построения графиков и диаграмм

### Инфраструктура и инструменты:
- **Docker** — контейнеризация приложений
- **Docker Compose** — оркестрация множественных контейнеров
- **PostgreSQL 15** — реляционная база данных для хранения данных
- **Redis** — брокер сообщений для Celery и кеширование
- **Microsoft Edge Browser** — браузер для Selenium WebDriver
- **Microsoft Edge WebDriver** — драйвер для автоматизации Edge
- **Vercel** — хостинг и деплой frontend приложения
- **Telegram Bot API** — аутентификация пользователей через Telegram Web App

### Внешние API и сервисы:
- **Telegram Web App API** — интеграция с Telegram для аутентификации и UI
- **Wildberries API** — внутренние API маркетплейса для получения данных о товарах
- **AI API (artemox.com)** — внешний сервис для AI-анализа отзывов (модель deepseek-chat)

---

## Структура проекта

```
UB/
├── backend/                          # Backend сервисы (Python/FastAPI)
│   ├── __pycache__/                 # Кеш скомпилированных Python файлов
│   ├── parser_service.py            # Сервис парсинга Wildberries (Selenium + API)
│   ├── tasks.py                     # Celery задачи (парсинг, анализ, обновление)
│   ├── analysis_service.py          # Сервис расчета метрик и AI-анализа отзывов
│   ├── main.py                      # FastAPI приложение (все API endpoints)
│   ├── auth_service.py              # Сервис аутентификации через Telegram Web App
│   ├── database.py                  # Модели SQLAlchemy и настройка подключения к БД
│   ├── celery_app.py                # Конфигурация Celery (Redis, задачи, расписание)
│   ├── requirements.txt             # Python зависимости
│   ├── Dockerfile                   # Docker образ для backend (Python, Edge, WebDriver)
│   ├── msedgedriver                 # Бинарный файл Edge WebDriver
│   └── proxy_ext/                   # Временные файлы расширений браузера
│       └── proxy_auth_plugin.zip    # Автогенерируемое расширение для прокси
│
├── frontend/                        # Frontend приложение (React/Vite)
│   ├── src/
│   │   ├── App.jsx                  # Главный компонент приложения (роутинг, страницы)
│   │   ├── main.jsx                 # Точка входа React приложения
│   │   └── index.css                # Глобальные стили (Tailwind, Telegram themes)
│   ├── index.html                   # HTML шаблон (Telegram Web App SDK)
│   ├── package.json                 # NPM зависимости и скрипты
│   ├── tailwind.config.js           # Конфигурация Tailwind CSS
│   ├── postcss.config.js            # Конфигурация PostCSS (Tailwind, Autoprefixer)
│   └── vercel.json                  # Конфигурация деплоя на Vercel (SPA routing)
│
├── docker-compose.yml               # Docker Compose конфигурация (все сервисы)
├── package.json                     # Корневые зависимости (recharts)
└── PROJECT_DESCRIPTION.md           # Документация проекта (этот файл)
```

### Описание основных директорий:

**backend/** — содержит весь backend код на Python:
- FastAPI приложение с REST API
- Celery задачи для асинхронной обработки
- Модели базы данных
- Сервисы парсинга и анализа

**frontend/** — содержит весь frontend код на React:
- Компоненты React для UI
- Конфигурация сборки (Vite)
- Стили (Tailwind CSS)

**proxy_ext/** — временная директория для расширений браузера (автогенерируется)

---

## Текущий статус

### ✅ Что готово и работает:

1. **Парсинг данных Wildberries:**
   - Гибридный подход: статические данные через API (card.json) + динамические цены через Selenium
   - Автоматический поиск корзины товара (brute-force по всем корзинам 01-25)
   - Получение названия, бренда, изображения, цен (кошелек, обычная, базовая)
   - Парсинг отзывов через внутренние API WB (feedbacks1.wb.ru, feedbacks-api.wildberries.ru)
   - Обход блокировок через прокси с ротацией сессий
   - Fallback методы при неудаче основных способов получения данных

2. **Мониторинг цен:**
   - Добавление товаров в мониторинг с проверкой лимитов тарифов
   - Автоматическое сохранение истории цен в базу данных
   - Автоматическое обновление всех товаров каждый час (Celery Beat)
   - Отображение истории цен с графиками (Recharts)
   - Удаление товаров из мониторинга

3. **AI-анализ отзывов:**
   - Сбор отзывов с Wildberries (до 100 штук в зависимости от тарифа)
   - Анализ через внешний AI API (artemox.com, модель deepseek-chat)
   - Выявление 3 главных минусов товара
   - Генерация 5 стратегических советов для продавца
   - Сохранение результатов анализа в историю

4. **Пользовательская система:**
   - Аутентификация через Telegram Web App (HMAC-SHA256 валидация)
   - Многопользовательская архитектура с изоляцией данных
   - Тарифные планы: Free (3 товара), PRO (50 товаров), Business (500 товаров)
   - Система администраторов (определяется по username)
   - Режим отладки (DEBUG_MODE)

5. **Frontend интерфейс:**
   - 6 основных страниц: Главная, Цены (мониторинг), Добавить товар, ИИ анализ, История, Профиль
   - Нижняя навигация с 6 вкладками
   - Модальные окна для графиков и деталей
   - Адаптивный дизайн для Telegram Web App
   - Поддержка темного/светлого режима через CSS переменные Telegram
   - Polling статусов задач с обновлением в реальном времени

6. **API endpoints:**
   - Пользовательские: `/api/user/me`, `/api/user/tariffs`, `/api/user/history`
   - Мониторинг: `/api/monitor/add/{sku}`, `/api/monitor/list`, `/api/monitor/delete/{sku}`, `/api/monitor/history/{sku}`, `/api/monitor/status/{task_id}`
   - AI: `/api/ai/analyze/{sku}`, `/api/ai/result/{task_id}`
   - Отчеты: `/api/report/pdf/{sku}` (только для PRO и Business)
   - Админ: `/api/admin/users`, `/api/admin/set_plan`, `/api/admin/stats`
   - Платежи: `/api/payment/create`

7. **База данных:**
   - Модели: User, MonitoredItem, PriceHistory, SearchHistory
   - Асинхронные запросы для FastAPI (asyncpg)
   - Синхронные запросы для Celery (psycopg2-binary)
   - Каскадное удаление связанных записей
   - Индексы на telegram_id и sku для быстрого поиска

8. **Инфраструктура:**
   - Docker Compose конфигурация с 5 сервисами (redis, db, api, worker, beat)
   - Health checks для PostgreSQL
   - Автоматический рестарт контейнеров
   - Персистентное хранилище для БД (volumes)
   - Готовность к деплою на production

### 🔄 Что делаем сейчас / В разработке:

1. **Улучшение стабильности парсинга:**
   - Оптимизация поиска корзины товара (приоритизация новых корзин 18-25)
   - Улучшение обработки ошибок при парсинге
   - Расширение fallback методов получения данных

2. **Производительность:**
   - Оптимизация запросов к базе данных (eager loading, индексы)
   - Кеширование часто запрашиваемых данных
   - Оптимизация Selenium (уменьшение времени ожидания, параллелизм)

3. **Функциональность:**
   - Система уведомлений о изменении цен (Telegram)
   - Расширенная аналитика (тренды, прогнозы)
   - API для внешних интеграций
   - Экспорт данных в различные форматы (Excel, CSV)

4. **UI/UX улучшения:**
   - Улучшение отображения графиков
   - Добавление фильтров и сортировок
   - Улучшение мобильной версии
   - Добавление анимаций и переходов

---

## Правила кодирования

### Общие принципы:

1. **Комментарии:**
   - Комментарии пишутся на **русском языке** для объяснения бизнес-логики и сложных алгоритмов
   - Docstrings для функций и классов также на русском языке
   - Инлайн комментарии используют русский язык
   - Примеры:
     ```python
     # Получаем статику (асинхронно запускаем цикл для requests)
     static_info = {"name": f"Товар {sku}", "brand": "Unknown"}
     
     """
     Синхронный метод анализа отзывов.
     """
     def analyze_reviews_with_ai(self, reviews: list, product_name: str):
     ```

2. **Именование переменных и функций:**
   - Python: snake_case для функций и переменных (например: `get_product_data`, `save_price_to_db`)
   - Python: PascalCase для классов (например: `SeleniumWBParser`, `AuthService`)
   - JavaScript: camelCase для функций и переменных (например: `handleScan`, `fetchList`)
   - JavaScript: PascalCase для компонентов React (например: `HomePage`, `TariffCard`)
   - Константы: UPPER_SNAKE_CASE (например: `API_URL`, `ADMIN_USERNAME`)

3. **Типизация:**
   - Python: используются type hints где возможно (например: `sku: int`, `-> bool`)
   - Python: опциональные типы через `Optional` или `|` (Python 3.10+)
   - JavaScript: типизация не используется (нет TypeScript), но желательно добавлять JSDoc комментарии для сложных функций

4. **Форматирование кода:**
   - Python: следуем PEP 8 (4 пробела для отступов, максимальная длина строки 120 символов)
   - JavaScript: следуем стандартам ESLint (отступы 2 пробела, одинарные кавычки предпочтительны)
   - Автоматическое форматирование не настроено, форматирование вручную

5. **Структура кода:**
   - Каждый файл должен иметь одну четкую ответственность
   - Функции должны быть небольшими и делать одну вещь
   - Избегаем глубокой вложенности (максимум 3-4 уровня)
   - Разделяем импорты: стандартная библиотека, сторонние библиотеки, локальные модули (с пустыми строками)

6. **Обработка ошибок:**
   - Всегда используем try-except блоки для внешних вызовов (API, БД, файлы)
   - Логируем все ошибки с контекстом (используем logger)
   - Возвращаем понятные сообщения об ошибках пользователю
   - Не используем голые `except:` без указания типа исключения (предпочтительно `except Exception as e:`)

7. **Работа с асинхронностью:**
   - FastAPI endpoints: используем `async def` для всех endpoints
   - База данных: используем `AsyncSession` в FastAPI, `SyncSession` в Celery задачах
   - Celery задачи: синхронные функции, но могут создавать event loop для асинхронных операций
   - HTTP запросы: `aiohttp` для асинхронных, `requests` для синхронных контекстов

8. **Работа с базой данных:**
   - Всегда закрываем сессии после использования (через `finally` или контекстный менеджер)
   - Используем транзакции для связанных операций
   - Проверяем наличие данных перед обращением к атрибутам
   - Используем `selectinload` для жадной загрузки связанных объектов при необходимости

9. **Логирование:**
   - Используем модуль `logging` вместо `print`
   - Настраиваем разные уровни логирования (INFO, WARNING, ERROR)
   - Добавляем контекст в логи (SKU, user_id, task_id)
   - Пример: `logger.info(f"--- АНАЛИЗ ЦЕН SKU: {sku} ---")`

10. **Безопасность:**
    - Никогда не логируем чувствительные данные (токены, пароли, прокси)
    - Валидируем все входные данные
    - Используем параметризованные запросы к БД (защита от SQL injection)
    - Проверяем права доступа перед выполнением операций

11. **Конфигурация:**
    - Все конфигурационные значения выносим в переменные окружения
    - Используем `.env` файлы для локальной разработки
    - Никогда не коммитим `.env` файлы в репозиторий
    - Используем значения по умолчанию для опциональных параметров

12. **Тестирование:**
    - Тесты не написаны (пока в разработке)
    - Планируется добавление unit-тестов для критичных функций
    - Интеграционные тесты для API endpoints

13. **Git:**
    - Коммиты на русском языке с описанием изменений
    - Примеры: "Fix syntax error", "Global update", "bugs fix"
    - Используются ветки для разработки новых функций

14. **Документация:**
    - Код должен быть самодокументируемым (понятные имена переменных и функций)
    - Сложная логика обязательно комментируется
    - API endpoints документируются через FastAPI автоматическую документацию
    - Этот файл содержит полную документацию проекта

---

## Дополнительные детали

### Переменные окружения:

Проект использует файл `.env` (не включен в репозиторий):

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token

# Прокси настройки
PROXY_USER=your_proxy_username
PROXY_PASS=your_proxy_password
PROXY_HOST=your_proxy_host
PROXY_PORT=your_proxy_port

# Режим браузера
HEADLESS=True

# База данных
DATABASE_URL=postgresql+asyncpg://postgres:wb_secret_password@db:5432/wb_monitor

# Redis
REDIS_URL=redis://redis:6379/0

# AI API
AI_API_KEY=your_ai_api_key

# Режим отладки (опционально)
DEBUG_MODE=False
```

### Запуск проекта:

#### Docker Compose (рекомендуется):
```bash
docker-compose up -d
```

#### Локальная разработка:
```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload

# Frontend
cd frontend
npm install
npm run dev
```

### API документация:

FastAPI автоматически генерирует документацию:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

---

**Дата последнего обновления:** 2026  
**Версия документа:** 2.0
